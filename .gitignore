# Conversation data / sensitive
projects/
sessions/
history.jsonl
shell-snapshots/

# Ephemeral / cache / generated state
backups/
cache/
debug/
file-history/
telemetry/
tasks/
plans/
stats-cache.json
mcp-needs-auth-cache.json
policy-limits.json
remote-settings.json
.last-cleanup
.last-update-result.json
*.swp

# Don't push plugin state at all — installs/marketplaces are personal/
# machine-specific and shouldn't live in this repo
plugins/

# Skill run output, not a skill definition
skills/vdb-review-workspace/

# OS cruft
.DS_Store
